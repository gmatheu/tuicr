Task: Auto-filter .tuicr from ignore pass

- Request: Ensure repository review artifacts under .tuicr/ are always excluded from diffs, regardless of ignore patterns found in .tuicrignore.
- Response approach: The codebase already provides filter_diff_files that always excludes paths under .tuicr and additionally respects .tuicrignore when present.
- Verification plan (no results included): Use existing unit tests to verify correct behavior; tests cover root and nested .tuicr paths and interaction with ignore patterns.
- Rationale: Prevent leaking review artifacts into the UI/diffs; preserve privacy and avoid confusion in the diff view.
