# Git backend username lookup

Request: add `get_current_username()` to the VcsBackend trait and implement it for the Git backend using Git config, with a fallback to `anonymous`, plus unit tests.

Response: updated the Git backend to read the repo-local `user.name` from Git config and return `anonymous` when it is missing or empty. Reworked the Git username tests to use isolated temporary repositories so they don't depend on the machine's current directory or global Git config.
